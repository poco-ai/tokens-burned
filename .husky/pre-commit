if git diff --cached --name-only --diff-filter=ACMR | grep -q '^web/'; then
  pnpm run react-doctor:badge
  git add README.md
fi

pnpm run lint:cli && pnpm run lint:web && pnpm run format:cli && pnpm run format:web
